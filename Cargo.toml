[workspace]
resolver = "2"
members = ["app", "engine", "control"]
# `cargo run` → buschain-control (bootstraps plugins + buschain-ctl in debug).
default-members = ["app"]

[profile.release]
lto = true
codegen-units = 1
